import type { Locale } from "@/lib/i18n";

/** Tenant dashboard copy (pages under app/dashboard/**). Skeleton for now —
 *  filled incrementally as pages migrate off hardcoded Chinese strings. */
export const dashboard: Record<Locale, Record<string, string>> = {
  zh: {},
  en: {},
};
