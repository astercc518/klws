import type { Locale } from "@/lib/i18n";

/** Auth flow copy (login/register/forgot-password/reset-password). Skeleton
 *  for now — filled incrementally as pages migrate off hardcoded Chinese
 *  strings. */
export const auth: Record<Locale, Record<string, string>> = {
  zh: {},
  en: {},
};
