# M10 安全/合规 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地安全与合规底座:信封加密 Cipher(AES-256-GCM,KEK 包裹分租户 DEK,版本轮换,crypto-shredding)、PII 加密 + 盲索引(双写,暂留明文)、Postgres RLS 双角色 + `WithTenant`(机制完整,billing 路径接入)、`suppression_list`(发送前合规拦截)、append-only `audit_log`、数据驻留 Router 桩。

**Architecture:** 全新 `internal/crypto` 包提供信封加密(主密钥 KEK 来自 env 包裹每租户随机 DEK,密文带版本前缀;删 DEK 即 crypto-shred)与 HMAC 盲索引。RLS 用两个物理分离连接池:`app_tenant`(NOSUPERUSER、受 RLS 约束、每事务 `SET LOCAL app.current_tenant_id`)与 `app_system`(BYPASSRLS、跨租户后台作业);`Manager.WithTenant(ctx,tenantID)` 开启租户事务。PII 列双写(明文 + `*_enc` 密文 + `phone_bidx` 盲索引),去重改用盲索引唯一索引,明文列暂留(收缩留 M11)。suppression 在 `dispatchBatch` 拉取时按盲索引拦截置 `skipped`。审计写入 append-only `audit_log`。

**Tech Stack:** Go 1.26.4 stdlib `crypto/aes`+`crypto/cipher`+`crypto/hmac`+`crypto/sha256`+`crypto/rand`、pgx/v5(RLS/SET LOCAL/事务)、Postgres RLS。

## Global Constraints

- **密钥分层**:主密钥 KEK 仅来自 env `WADIST_MASTER_KEY`(base64 32 字节);每租户 DEK 随机 32 字节,以 AES-256-GCM 被 KEK 包裹后存 `tenant_keys.key_enc`;密文格式 `version(4B big-endian) || nonce(12B) || ciphertext+tag`。盲索引用独立 env `WADIST_BLIND_INDEX_KEY`(base64 32 字节)的 HMAC-SHA256,绝不复用 KEK。
- **crypto-shredding**:删除某租户的全部 `tenant_keys` 行即令其 PII 密文不可恢复(GDPR 删除权,无需逐行删数据)。Shred 后 `Decrypt` 必须返回明确错误(`ErrKeyNotFound`)。
- **版本轮换**:`Rotate` 生成新版本 DEK,旧版本保留以解旧密文;`Decrypt` 按密文内 version 取对应 DEK。
- **RLS 正确性**:`app_tenant` 角色 NOSUPERUSER 且无 BYPASSRLS;9 张租户表 `ENABLE` + `FORCE ROW LEVEL SECURITY`,策略 `USING (tenant_id = current_setting('app.current_tenant_id', true)::bigint)`(`true` 使缺设返回 NULL→零行,不报错)。`WithTenant` 在事务内 `SET LOCAL`,跨事务不泄漏。`app_system` 用于跨租户作业(metrics 聚合、对账、节点心跳、接管),BYPASSRLS。**本里程碑仅把 billing 的租户路径接入 RLS;dispatch/sendgate/store/metrics 的迁移留文档化分期债**(见 Task 2 末尾 TODO 清单)。
- **PII 双写**:`campaign_recipients` 与 `account_devices` 插入时同时写明文 + `*_enc`(密文)+(recipients)`phone_bidx`(盲索引);去重改用 `UNIQUE(campaign_id, phone_bidx)`;**明文列与旧 UNIQUE 暂保留**(收缩阶段 M11 再删),避免破坏现有去重/读取路径。
- **审计 append-only**:`audit_log` 仅追加;通过 `REVOKE UPDATE, DELETE` on audit_log FROM app_tenant/app_system + 不暴露更新 API 保证。`AuditWriter.Record` 在 billing 退款审批/拒绝、密钥轮换/shred、suppression 增删处写入。
- 向后兼容:RLS 与 PII 接入为**加性**——既有 billing/dispatch 测试构造方式不传新依赖时行为不变(tenant-tx beginner 为 nil 时回退到原 pool;PII 双写在缺 Cipher 时跳过密文列)。新增专门的 RLS 隔离测试 / PII 往返测试证明能力。
- ctx 贯穿;`%w`;SQL 仅 `$N`;`-race`;集成测试真实 PG16;短模式 skip;高基数 metric 门禁保持绿。
- module `github.com/acme/wadist`,Go 1.26.4,`/usr/local/bin/go`。迁移新增 `0008_security.sql`。

---

## File Structure

- `internal/crypto/cipher.go` — `Cipher`(Encrypt/Decrypt/Rotate)、信封格式、`ErrKeyNotFound`。
- `internal/crypto/keyrepo.go` — `KeyRepo`(GetCurrentDEK/GetDEK/CreateTenantKey/Shred,KEK 包裹)。
- `internal/crypto/blindindex.go` — `BlindIndex(value string) []byte`(HMAC-SHA256)。
- `internal/crypto/*_test.go` — 往返、轮换、shred、盲索引一致性、版本解码(真实 PG for KeyRepo)。
- `migrations/0008_security.sql` — tenant_keys、PII 列、RLS 角色+策略、suppression_list、audit_log。
- `internal/config/config.go` — MasterKey、BlindIndexKey([]byte 解码)、AppTenantDSN、AppSystemDSN、NodeRegion。
- `internal/store/rls.go` — `WithTenant(ctx,tenantID)(pgx.Tx,error)`、tenantPool/systemPool 接线、`SystemPool()`。
- `internal/store/rls_test.go` — RLS 跨租户隔离(真实 app_tenant 角色)。
- `internal/billing/billing.go` — 可选 tenant-tx beginner 注入,Hold/Settle/RequestRefund 走 WithTenant。
- `internal/billing/rls_test.go` — billing-under-RLS 隔离。
- `internal/store/pii.go` — 双写插入辅助(AddRecipientPII / SetDevicePhonePII)。
- `internal/store/pii_test.go` — PII 双写 + 盲索引去重(真实 PG)。
- `internal/store/suppression.go` — `AddSuppression`、dispatchBatch 拦截。
- `internal/dispatch/batch.go` — 拉取时盲索引 suppression 拦截 → `skipped`。
- `internal/audit/audit.go` — `AuditWriter.Record` + append-only。
- `internal/crypto/router.go` — 数据驻留 `Router`(region→pool 桩)。
- `cmd/wadist/main.go` — 装配 Cipher/KeyRepo/AuditWriter/RLS 池/Router。

---

### Task 1: Cipher 信封加密 + tenant_keys + 盲索引(greenfield)

**Files:**
- Create: `internal/crypto/cipher.go`, `keyrepo.go`, `blindindex.go`
- Create: `internal/crypto/cipher_test.go`, `keyrepo_test.go`, `blindindex_test.go`
- Create: `migrations/0008_security.sql`(本任务仅含 tenant_keys 部分;后续任务追加其它对象到同一文件)
- Modify: `internal/config/config.go`(MasterKey、BlindIndexKey)

**Interfaces:**
- Produces:
  - `var ErrKeyNotFound = errors.New("crypto: tenant key not found (shredded?)")`
  - `type KeyRepo struct{...}`;`func NewKeyRepo(pool *pgxpool.Pool, masterKey []byte) (*KeyRepo, error)`;`GetCurrentDEK(ctx, tenantID int64) (dek []byte, version int32, err error)`;`GetDEK(ctx, tenantID int64, version int32) ([]byte, error)`;`CreateTenantKey(ctx, tenantID int64) (version int32, err error)`;`Shred(ctx, tenantID int64) error`。
  - `type Cipher struct{...}`;`func NewCipher(kr *KeyRepo) *Cipher`;`Encrypt(ctx, tenantID int64, plaintext []byte) ([]byte, error)`;`Decrypt(ctx, tenantID int64, blob []byte) ([]byte, error)`;`Rotate(ctx, tenantID int64) (int32, error)`。
  - `func BlindIndex(key []byte, value string) []byte`(HMAC-SHA256,32 字节)。
  - config `MasterKey []byte`、`BlindIndexKey []byte`。

- [ ] **Step 1: config 密钥解码 + 测试**

`config.go`:增 `MasterKey []byte`、`BlindIndexKey []byte`;`Load()` 读 env `WADIST_MASTER_KEY`/`WADIST_BLIND_INDEX_KEY`(base64 std 解码,必须正好 32 字节,否则返回错误——但为不破坏既有无密钥的测试/启动,**仅当 env 非空时解码并校验长度;为空时留 nil**,由使用方在需要加密时报错)。加 `TestLoad_DecodesKeys`(设两个 32 字节 base64 env,断言 len==32;空 env → nil 无错)。

- [ ] **Step 2: 迁移 tenant_keys**

`migrations/0008_security.sql`(起始,后续任务在同文件追加):

```sql
-- 0008_security.sql — encryption keys, PII columns, RLS, suppression, audit.

-- ── tenant_keys: KEK-wrapped per-tenant data keys (DEKs); delete = crypto-shred ──
CREATE TABLE IF NOT EXISTS tenant_keys (
    tenant_id  BIGINT NOT NULL,
    version    INT    NOT NULL,
    key_enc    BYTEA  NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY (tenant_id, version)
);
```

- [ ] **Step 3: blindindex.go + 测试**

```go
package crypto

import (
	"crypto/hmac"
	"crypto/sha256"
)

// BlindIndex returns a deterministic HMAC-SHA256 of value under key. It enables
// equality lookup (dedup, suppression match) over encrypted columns without
// decrypting. key must be 32 bytes; value should be normalized by the caller
// (e.g. E.164 phone). Never use the master KEK as the blind-index key.
func BlindIndex(key []byte, value string) []byte {
	m := hmac.New(sha256.New, key)
	m.Write([]byte(value))
	return m.Sum(nil)
}
```

测试 `blindindex_test.go`:同 key+value 确定一致;不同 value 不同;长度 32。

- [ ] **Step 4: cipher.go(信封格式)+ 测试**

```go
package crypto

import (
	"context"
	"crypto/aes"
	"crypto/cipher"
	"crypto/rand"
	"encoding/binary"
	"errors"
	"fmt"
)

var ErrKeyNotFound = errors.New("crypto: tenant key not found (shredded?)")

// Cipher does envelope AES-256-GCM: per-tenant DEKs (from KeyRepo) encrypt data;
// the blob layout is version(4B BE) || nonce(12B) || ciphertext+tag. Decrypt reads
// the version to fetch the matching DEK, so rotated ciphertext stays readable.
type Cipher struct{ kr *KeyRepo }

func NewCipher(kr *KeyRepo) *Cipher { return &Cipher{kr: kr} }

func (c *Cipher) Encrypt(ctx context.Context, tenantID int64, plaintext []byte) ([]byte, error) {
	dek, version, err := c.kr.GetCurrentDEK(ctx, tenantID)
	if err != nil {
		return nil, err
	}
	gcm, err := newGCM(dek)
	if err != nil {
		return nil, err
	}
	nonce := make([]byte, gcm.NonceSize())
	if _, err := rand.Read(nonce); err != nil {
		return nil, fmt.Errorf("nonce: %w", err)
	}
	out := make([]byte, 4)
	binary.BigEndian.PutUint32(out, uint32(version))
	out = append(out, nonce...)
	return gcm.Seal(out, nonce, plaintext, nil), nil
}

func (c *Cipher) Decrypt(ctx context.Context, tenantID int64, blob []byte) ([]byte, error) {
	if len(blob) < 4+12 {
		return nil, fmt.Errorf("crypto: ciphertext too short")
	}
	version := int32(binary.BigEndian.Uint32(blob[:4]))
	dek, err := c.kr.GetDEK(ctx, tenantID, version)
	if err != nil {
		return nil, err
	}
	gcm, err := newGCM(dek)
	if err != nil {
		return nil, err
	}
	ns := gcm.NonceSize()
	nonce := blob[4 : 4+ns]
	ct := blob[4+ns:]
	pt, err := gcm.Open(nil, nonce, ct, nil)
	if err != nil {
		return nil, fmt.Errorf("crypto: decrypt: %w", err)
	}
	return pt, nil
}

func (c *Cipher) Rotate(ctx context.Context, tenantID int64) (int32, error) {
	return c.kr.CreateTenantKey(ctx, tenantID)
}

func newGCM(key []byte) (cipher.AEAD, error) {
	blk, err := aes.NewCipher(key) // 32 bytes -> AES-256
	if err != nil {
		return nil, fmt.Errorf("aes: %w", err)
	}
	return cipher.NewGCM(blk)
}
```

测试 `cipher_test.go`(用真实 PG-backed KeyRepo,见 Step 5;或先用内存 fake KeyRepo——建议真实 PG 以验证端到端):往返(Encrypt→Decrypt==原文);轮换后旧密文仍可解(Rotate 产生新版本,旧 blob 仍解);shred 后 Decrypt→ErrKeyNotFound;两租户密文不可互解。

- [ ] **Step 5: keyrepo.go(KEK 包裹 DEK)+ 测试**

```go
package crypto

import (
	"context"
	"crypto/rand"
	"errors"
	"fmt"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgxpool"
)

// KeyRepo manages per-tenant DEKs wrapped by the master KEK. The wrapped key is
// itself an AES-256-GCM blob (nonce||ct) so the KEK never touches plaintext DEKs
// at rest. Shred deletes a tenant's keys, making its ciphertext unrecoverable.
type KeyRepo struct {
	pool *pgxpool.Pool
	kek  []byte // 32 bytes
}

func NewKeyRepo(pool *pgxpool.Pool, masterKey []byte) (*KeyRepo, error) {
	if len(masterKey) != 32 {
		return nil, fmt.Errorf("crypto: master key must be 32 bytes, got %d", len(masterKey))
	}
	return &KeyRepo{pool: pool, kek: masterKey}, nil
}

func (k *KeyRepo) wrap(dek []byte) ([]byte, error) {
	gcm, err := newGCM(k.kek)
	if err != nil {
		return nil, err
	}
	nonce := make([]byte, gcm.NonceSize())
	if _, err := rand.Read(nonce); err != nil {
		return nil, err
	}
	return gcm.Seal(nonce, nonce, dek, nil), nil
}

func (k *KeyRepo) unwrap(blob []byte) ([]byte, error) {
	gcm, err := newGCM(k.kek)
	if err != nil {
		return nil, err
	}
	ns := gcm.NonceSize()
	if len(blob) < ns {
		return nil, fmt.Errorf("crypto: wrapped key too short")
	}
	return gcm.Open(nil, blob[:ns], blob[ns:], nil)
}

func (k *KeyRepo) CreateTenantKey(ctx context.Context, tenantID int64) (int32, error) {
	dek := make([]byte, 32)
	if _, err := rand.Read(dek); err != nil {
		return 0, fmt.Errorf("dek: %w", err)
	}
	wrapped, err := k.wrap(dek)
	if err != nil {
		return 0, err
	}
	var version int32
	err = k.pool.QueryRow(ctx, `
INSERT INTO tenant_keys (tenant_id, version, key_enc)
VALUES ($1, COALESCE((SELECT max(version) FROM tenant_keys WHERE tenant_id=$1), 0) + 1, $2)
RETURNING version`, tenantID, wrapped).Scan(&version)
	if err != nil {
		return 0, fmt.Errorf("create tenant key: %w", err)
	}
	return version, nil
}

func (k *KeyRepo) GetCurrentDEK(ctx context.Context, tenantID int64) ([]byte, int32, error) {
	var wrapped []byte
	var version int32
	err := k.pool.QueryRow(ctx, `
SELECT key_enc, version FROM tenant_keys
 WHERE tenant_id=$1 ORDER BY version DESC LIMIT 1`, tenantID).Scan(&wrapped, &version)
	if errors.Is(err, pgx.ErrNoRows) {
		return nil, 0, ErrKeyNotFound
	}
	if err != nil {
		return nil, 0, fmt.Errorf("get current dek: %w", err)
	}
	dek, err := k.unwrap(wrapped)
	if err != nil {
		return nil, 0, fmt.Errorf("unwrap dek: %w", err)
	}
	return dek, version, nil
}

func (k *KeyRepo) GetDEK(ctx context.Context, tenantID int64, version int32) ([]byte, error) {
	var wrapped []byte
	err := k.pool.QueryRow(ctx,
		`SELECT key_enc FROM tenant_keys WHERE tenant_id=$1 AND version=$2`, tenantID, version).Scan(&wrapped)
	if errors.Is(err, pgx.ErrNoRows) {
		return nil, ErrKeyNotFound
	}
	if err != nil {
		return nil, fmt.Errorf("get dek v%d: %w", version, err)
	}
	return k.unwrap(wrapped)
}

func (k *KeyRepo) Shred(ctx context.Context, tenantID int64) error {
	_, err := k.pool.Exec(ctx, `DELETE FROM tenant_keys WHERE tenant_id=$1`, tenantID)
	if err != nil {
		return fmt.Errorf("shred tenant keys: %w", err)
	}
	return nil
}
```

测试 `keyrepo_test.go`(真实 PG):CreateTenantKey 递增版本;GetCurrentDEK 取最新;往返 wrap/unwrap 得原 DEK(32 字节);Shred 后 GetCurrentDEK→ErrKeyNotFound;两租户 DEK 不同。用一个固定 32 字节测试 KEK。

- [ ] **Step 6: 跑全部 crypto 测试 + 迁移**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/crypto/ ./internal/config/ -race -v`;`/usr/local/bin/go build ./...`;`/usr/local/bin/go vet ./internal/crypto/`。
Expected: 全 PASS。

- [ ] **Step 7: 提交**

```bash
git add internal/crypto/ internal/config/ migrations/0008_security.sql
git commit -m "feat(crypto): AES-256-GCM envelope Cipher + tenant_keys KeyRepo (crypto-shred, rotation) + blind index"
```

---

### Task 2: RLS 双角色 + WithTenant(机制完整,billing 接入)

**Files:**
- Modify: `migrations/0008_security.sql`(追加角色 + RLS 策略)
- Modify: `internal/config/config.go`(AppTenantDSN/AppSystemDSN)
- Modify: `internal/store/manager.go`(tenantPool/systemPool 接线)
- Create: `internal/store/rls.go`(WithTenant、SystemPool)
- Create: `internal/store/rls_test.go`
- Modify: `internal/billing/billing.go`(可选 tenant-tx beginner)
- Create: `internal/billing/rls_test.go`

**Interfaces:**
- Produces:
  - config `AppTenantDSN, AppSystemDSN string`。
  - `func (m *Manager) WithTenant(ctx context.Context, tenantID int64) (pgx.Tx, error)`(从 tenantPool 取连接、BEGIN、`SET LOCAL app.current_tenant_id = $1`,返回 tx;调用方负责 Commit/Rollback)。
  - `func (m *Manager) SystemPool() *pgxpool.Pool`、`func (m *Manager) TenantPool() *pgxpool.Pool`。
  - `type TenantTxBeginner interface { WithTenant(ctx context.Context, tenantID int64) (pgx.Tx, error) }`(Manager 满足)。
  - billing:`func (r *Repo) UseTenantRLS(b TenantTxBeginner) *Repo`(可选注入;设置后 Hold/Settle/RequestRefund 用 RLS 事务,否则回退 r.pool)。

- [ ] **Step 1: 迁移追加角色 + RLS 策略**

在 `0008_security.sql` 追加:

```sql
-- ── RLS roles (idempotent) ──
DO $$ BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname='app_tenant') THEN
    CREATE ROLE app_tenant NOSUPERUSER NOINHERIT LOGIN PASSWORD 'app_tenant_pw';
  END IF;
  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname='app_system') THEN
    CREATE ROLE app_system NOSUPERUSER NOINHERIT LOGIN PASSWORD 'app_system_pw' BYPASSRLS;
  END IF;
END $$;

GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_tenant, app_system;
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO app_tenant, app_system;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO app_tenant, app_system;
-- audit_log is append-only even for these roles (see Task 4):
-- (REVOKE UPDATE, DELETE ON audit_log added in Task 4 after the table exists)

-- ── enable RLS + tenant-isolation policy on the 9 tenant-scoped tables ──
DO $$
DECLARE t text;
BEGIN
  FOREACH t IN ARRAY ARRAY[
    'account_devices','tenant_wallets','billing_charges','wallet_ledger',
    'refund_requests','reconciliation_runs','campaign_templates','campaigns','campaign_recipients'
  ] LOOP
    EXECUTE format('ALTER TABLE %I ENABLE ROW LEVEL SECURITY', t);
    EXECUTE format('ALTER TABLE %I FORCE ROW LEVEL SECURITY', t);
    EXECUTE format('DROP POLICY IF EXISTS tenant_isolation ON %I', t);
    EXECUTE format($f$CREATE POLICY tenant_isolation ON %I
      USING (tenant_id = current_setting('app.current_tenant_id', true)::bigint)
      WITH CHECK (tenant_id = current_setting('app.current_tenant_id', true)::bigint)$f$, t);
  END LOOP;
END $$;
```

> **note:** `tenant_wallets` 的隔离列是 `tenant_id`(其 PK)。`FORCE` 让表 owner 也受约束;superuser 仍 bypass(故迁移与 system 池用 superuser/app_system 不受影响,RLS 仅约束 app_tenant)。`GRANT ... ON ALL TABLES` 必须在所有表已存在后执行——0008 在 0001-0007 之后运行,OK。

- [ ] **Step 2: config 双 DSN**

`config.go`:增 `AppTenantDSN, AppSystemDSN string`(env `WADIST_APP_TENANT_DSN`/`WADIST_APP_SYSTEM_DSN`,**为空时回退到 PostgresDSN**——单 DSN 开发仍可跑,但此时 tenantPool==bizPool 用 superuser,RLS 不强制;专门的 rls_test 显式建 app_tenant 角色+独立连接验证)。

- [ ] **Step 3: manager 接线 tenantPool/systemPool + rls.go**

`manager.go`:`Manager` 增字段 `tenantPool, systemPool *pgxpool.Pool`;`Init`/`newManager` 中:若 cfg.AppTenantDSN 非空则建独立 pool(角色 app_tenant),否则 `tenantPool = bizPool`;systemPool 同理(app_system 或回退 bizPool)。`Close()` 增关闭新池(注意避免重复关闭回退到 bizPool 的同一对象——用指针相等判断)。

`store/rls.go`:

```go
package store

import (
	"context"
	"fmt"

	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgxpool"
)

// WithTenant begins a transaction on the RLS-constrained tenant pool and sets the
// tenant id for the RLS policy (SET LOCAL is transaction-scoped). The caller MUST
// Commit or Rollback the returned tx. All queries on this tx see only rows where
// tenant_id matches.
func (m *Manager) WithTenant(ctx context.Context, tenantID int64) (pgx.Tx, error) {
	tx, err := m.tenantPool.Begin(ctx)
	if err != nil {
		return nil, fmt.Errorf("begin tenant tx: %w", err)
	}
	if _, err := tx.Exec(ctx, `SELECT set_config('app.current_tenant_id', $1, true)`, fmt.Sprint(tenantID)); err != nil {
		_ = tx.Rollback(ctx)
		return nil, fmt.Errorf("set tenant: %w", err)
	}
	return tx, nil
}

func (m *Manager) TenantPool() *pgxpool.Pool  { return m.tenantPool }
func (m *Manager) SystemPool() *pgxpool.Pool  { return m.systemPool }
```

> 用 `set_config(name, value, is_local=true)` 而非 `SET LOCAL`,便于参数化 tenantID(SET LOCAL 不支持参数占位符)。

- [ ] **Step 4: rls_test.go(跨租户隔离,真实 app_tenant 角色)**

`internal/store/rls_test.go`(真实 PG):
- 用 superuser 连接 apply 迁移(角色+策略已建)。
- 直接 superuser 插入两租户 account_devices(tenant 1、tenant 2)。
- 用 **app_tenant 角色**新建一个 pgxpool(DSN 改 user=app_tenant password=app_tenant_pw),`WithTenant`-风格:`tx.Exec("SELECT set_config('app.current_tenant_id','1',true)")`,`SELECT count(*) FROM account_devices` 必须只见 tenant 1 行;切 tenant 2 同理;不设 tenant → 零行。
- 写入越权校验:在 tenant=1 下 `INSERT account_devices(tenant_id=2,...)` 必须被 `WITH CHECK` 拒绝。

> implementer:从测试容器 DSN 改 user/password 构造 app_tenant DSN(同 host/port/db,改 user)。

- [ ] **Step 5: billing 可选 RLS 注入 + rls_test**

`billing.go`:`Repo` 增字段 `txb TenantTxBeginner`(nil 默认);`func (r *Repo) UseTenantRLS(b TenantTxBeginner) *Repo { r.txb = b; return r }`。把 Hold/Settle/RequestRefund 的事务获取改为:`if r.txb != nil { tx,err = r.txb.WithTenant(ctx, tenantID) } else { tx,err = r.pool.Begin(ctx) }`(其余逻辑不变;ApproveRefund/RejectRefund/对账保持 r.pool=system)。**既有 billing 测试不调用 UseTenantRLS → txb nil → 行为完全不变,零回归。**

`internal/billing/rls_test.go`:构造 Repo + `UseTenantRLS(manager)`,在 RLS 下做 Hold(tenant 1):成功;尝试以 tenant 1 的事务读 tenant 2 的钱包/charge → 零行(隔离生效)。证明 billing-under-RLS。

- [ ] **Step 6: 文档化分期债**

在 `billing.go` 顶部或 rls.go 加注释 TODO 清单:dispatch(13)/sendgate(3)/store-node(12,跨租户用 SystemPool)/metrics-dbcollector(6,跨租户聚合用 SystemPool)的 RLS 迁移为后续分期项;当前仅 billing 租户路径接入。

- [ ] **Step 7: 跑测试**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/store/ ./internal/billing/ ./internal/config/ -race -v`;build/vet。
Expected: 全 PASS(既有 billing 测试零回归 + 新 RLS 隔离测试通过)。

- [ ] **Step 8: 提交**

```bash
git add migrations/0008_security.sql internal/config/ internal/store/ internal/billing/
git commit -m "feat(store/billing): RLS dual-role + WithTenant tenant isolation; billing tenant path under RLS"
```

---

### Task 3: PII 加密 + 盲索引(双写,暂留明文)

**Files:**
- Modify: `migrations/0008_security.sql`(追加 PII 列 + 盲索引唯一索引)
- Create: `internal/store/pii.go`
- Create: `internal/store/pii_test.go`

**Interfaces:**
- Consumes: `crypto.Cipher`、`crypto.BlindIndex`、config.BlindIndexKey。
- Produces:
  - `func (m *Manager) AddRecipientPII(ctx, c *crypto.Cipher, blindKey []byte, tenantID, campaignID int64, phone, country string, vars []byte) (recipientID int64, err error)`(双写 phone 明文 + phone_enc + phone_bidx;去重靠 (campaign_id, phone_bidx) 唯一索引,冲突→返回既有 id 或 ErrDuplicateRecipient)。
  - `func (m *Manager) SetDevicePhonePII(ctx, c *crypto.Cipher, tenantID int64, accountJID, phone string) error`(写 account_devices.phone_number_enc;明文 phone_number 已有)。

- [ ] **Step 1: 迁移追加 PII 列**

`0008_security.sql` 追加:

```sql
-- ── PII at-rest encryption columns (dual-write; plaintext kept until M11 contract) ──
ALTER TABLE account_devices    ADD COLUMN IF NOT EXISTS phone_number_enc BYTEA;
ALTER TABLE campaign_recipients ADD COLUMN IF NOT EXISTS phone_enc BYTEA;
ALTER TABLE campaign_recipients ADD COLUMN IF NOT EXISTS phone_bidx BYTEA;
-- blind-index unique for dedup (coexists with the legacy UNIQUE(campaign_id, phone) until contract):
CREATE UNIQUE INDEX IF NOT EXISTS idx_recip_phone_bidx ON campaign_recipients (campaign_id, phone_bidx)
  WHERE phone_bidx IS NOT NULL;
```

- [ ] **Step 2: 写 pii.go**

实现 AddRecipientPII(在一个事务/单语句中 INSERT campaign_recipients 同时写 phone、phone_enc=Cipher.Encrypt(tenantID,phone)、phone_bidx=BlindIndex(blindKey,phone);ON CONFLICT (campaign_id, phone_bidx) WHERE phone_bidx IS NOT NULL DO NOTHING + RETURNING id,或冲突时 SELECT 既有 id)。SetDevicePhonePII 同理写 phone_number_enc。ctx + %w。要求 Cipher 非 nil(PII 必加密);blindKey 长度校验。

> implementer:读 campaign_recipients 既有列(0006:tenant_id,campaign_id,phone,country_code,vars,state...)与 NOT NULL 约束,INSERT 补齐。vars 为 jsonb/bytea——按既有列类型传。

- [ ] **Step 3: pii_test.go(真实 PG + 真实 Cipher/KeyRepo)**

- 建 KeyRepo(测试 KEK)+ CreateTenantKey(tenant 1)+ Cipher;blindKey 固定 32 字节。
- AddRecipientPII 插入 → 读回 phone_enc 用 Cipher.Decrypt 得原 phone;phone_bidx == BlindIndex(key,phone);明文 phone 也在。
- 重复同 (campaign,phone) → 盲索引唯一冲突被去重(返回既有 id 或 nil 不重复插)。
- 两租户同号 → phone_enc 不同(不同 DEK),但 phone_bidx 相同(全局盲索引键)——确认去重是 per-campaign 故不跨租户误去重(campaign 不同)。
- SetDevicePhonePII → 读回 phone_number_enc 解密得原值。

- [ ] **Step 4: 跑测试**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/store/ -run 'TestAddRecipientPII|TestSetDevicePhonePII|TestRecipientDedup' -race -v`;build/vet。

- [ ] **Step 5: 提交**

```bash
git add migrations/0008_security.sql internal/store/pii.go internal/store/pii_test.go
git commit -m "feat(store): PII phone encryption + blind-index dedup (dual-write, plaintext retained)"
```

---

### Task 4: suppression_list + audit_log(发送前合规拦截 + append-only 审计)

**Files:**
- Modify: `migrations/0008_security.sql`(suppression_list、audit_log + REVOKE)
- Create: `internal/audit/audit.go`, `internal/audit/audit_test.go`
- Create: `internal/store/suppression.go`, `internal/store/suppression_test.go`
- Modify: `internal/dispatch/batch.go`(拉取时 suppression 拦截 → skipped)
- Modify: `internal/billing/billing.go`(ApproveRefund/RejectRefund 写审计)

**Interfaces:**
- Produces:
  - `func (m *Manager) AddSuppression(ctx, blindKey []byte, tenantID int64, phone string) error`(写 phone_bidx)。
  - `type AuditWriter struct{...}`;`func NewAuditWriter(pool *pgxpool.Pool) *AuditWriter`;`type AuditEntry struct{ TenantID, ActorID, ResourceID int64; Action, ResourceType string; Details []byte }`;`func (w *AuditWriter) Record(ctx, e AuditEntry) error`。
  - batch.go suppression 拦截(SELECT 加 `AND phone_bidx NOT IN (suppression for this tenant)` 或 NOT EXISTS;被拦截的 recipient `UPDATE state='skipped'`)。

- [ ] **Step 1: 迁移追加 suppression_list + audit_log**

```sql
-- ── suppression list (do-not-contact), matched by phone blind index ──
CREATE TABLE IF NOT EXISTS suppression_list (
    id         BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    tenant_id  BIGINT NOT NULL,
    phone_bidx BYTEA  NOT NULL,
    reason     TEXT,
    added_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (tenant_id, phone_bidx)
);

-- ── append-only audit log ──
CREATE TABLE IF NOT EXISTS audit_log (
    id            BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    occurred_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
    tenant_id     BIGINT,
    actor_id      BIGINT,
    action        TEXT NOT NULL,
    resource_type TEXT,
    resource_id   BIGINT,
    details       JSONB
);

-- these two tables are created AFTER Task 2's GRANT ON ALL TABLES, so grant them
-- explicitly; then make audit_log append-only by revoking UPDATE/DELETE.
GRANT SELECT, INSERT ON suppression_list, audit_log TO app_tenant, app_system;
GRANT SELECT, USAGE ON ALL SEQUENCES IN SCHEMA public TO app_tenant, app_system;
REVOKE UPDATE, DELETE ON audit_log FROM app_tenant, app_system;
```

> **迁移顺序注意:** Task 2 的 `GRANT ... ON ALL TABLES` 在这两张表创建前运行,故此处必须显式 GRANT(否则 app_system 无法 INSERT audit_log/suppression)。identity 列需要 sequence 权限。`REVOKE UPDATE, DELETE` 仅在显式 GRANT 之后才有意义。

> suppression_list/audit_log 是否纳入 RLS?suppression 含 tenant_id 但发送拦截在 dispatchBatch(目前 system 池),按 tenant_id 显式过滤即可;本里程碑不强制其 RLS(留分期)。audit_log append-only 由 REVOKE 保证。

- [ ] **Step 2: audit.go + 测试**

`internal/audit/audit.go`:`AuditWriter.Record` INSERT 一行(ctx+%w)。`audit_test.go`(真实 PG):Record 写入可查;以 app_tenant/app_system 角色尝试 `UPDATE/DELETE audit_log` 被拒(权限错误)——证明 append-only(用专门角色连接)。

- [ ] **Step 3: suppression.go + 测试**

`AddSuppression`(写 (tenant_id, BlindIndex(key,phone)),ON CONFLICT DO NOTHING)。`suppression_test.go`:加入号码;按盲索引可查到;不同号码查不到。

- [ ] **Step 4: dispatchBatch suppression 拦截**

改 `batch.go` 拉取后:对每个 recipient,若其 `phone_bidx`(T3 已写)命中本租户 suppression_list → `UPDATE campaign_recipients SET state='skipped' WHERE id=$1`,不选号不入队不扣费。最简实现:拉取 SQL 增 `AND NOT EXISTS (SELECT 1 FROM suppression_list s WHERE s.tenant_id = <tenantID> AND s.phone_bidx = campaign_recipients.phone_bidx)` 以**不拉取**被压制者;另起一条把命中 suppression 的 pending 置 skipped(便于可观测)。或在 Go 侧逐个判定。implementer 选更简洁正确者并保证:被压制 recipient 最终为 `skipped`,绝不发送。补一个集成测试:一个被压制号码的 recipient 经 dispatchBatch 后为 skipped 且未入队。

> 注意 tenantID:dispatchBatch 已有 `tenantOf(campaignID)`,用之。

- [ ] **Step 5: billing 审计接线**

`ApproveRefund`/`RejectRefund`:成功后调用注入的 AuditWriter.Record(action="refund.approve"/"refund.reject", actor_id=adminID, resource_type="refund_request", resource_id=refundID, tenant from charge)。审计写入失败**不回滚业务**(best-effort + 记录),或在同事务内写(择一;建议同事务保证审计与业务原子)。AuditWriter 经可选注入(nil → 跳过,零回归既有测试)。补 billing 测试:ApproveRefund 后 audit_log 有对应行。

- [ ] **Step 6: 跑测试**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/audit/ ./internal/store/ ./internal/dispatch/ ./internal/billing/ -race -v`;build/vet/label-guard。
Expected: 全 PASS。

- [ ] **Step 7: 提交**

```bash
git add migrations/0008_security.sql internal/audit/ internal/store/suppression.go internal/store/suppression_test.go internal/dispatch/batch.go internal/billing/
git commit -m "feat(audit/dispatch): append-only audit_log + suppression_list skip in dispatchBatch"
```

---

### Task 5: 数据驻留 Router 桩 + main 装配

**Files:**
- Create: `internal/crypto/router.go`, `internal/crypto/router_test.go`
- Modify: `internal/config/config.go`(NodeRegion)
- Modify: `cmd/wadist/main.go`、`cmd/wadist/main_smoke_test.go`

**Interfaces:**
- Produces:
  - `type Router struct{...}`;`func NewRouter(def *pgxpool.Pool) *Router`;`func (r *Router) Register(region string, pool *pgxpool.Pool)`;`func (r *Router) PoolFor(region string) *pgxpool.Pool`(未知 region → default)。
  - config `NodeRegion string`(env `WADIST_NODE_REGION`,默认 `"default"`)。

- [ ] **Step 1: router.go + 测试**

```go
package crypto

import (
	"sync"

	"github.com/jackc/pgx/v5/pgxpool"
)

// Router maps a data-residency region to its database pool. For M10 it is a stub
// over a single default pool (single-region deployment); real per-region DSNs are
// wired in M11. PoolFor falls back to the default for unknown/empty regions.
type Router struct {
	mu    sync.RWMutex
	def   *pgxpool.Pool
	pools map[string]*pgxpool.Pool
}

func NewRouter(def *pgxpool.Pool) *Router {
	return &Router{def: def, pools: map[string]*pgxpool.Pool{}}
}
func (r *Router) Register(region string, pool *pgxpool.Pool) {
	r.mu.Lock(); defer r.mu.Unlock(); r.pools[region] = pool
}
func (r *Router) PoolFor(region string) *pgxpool.Pool {
	r.mu.RLock(); defer r.mu.RUnlock()
	if p, ok := r.pools[region]; ok { return p }
	return r.def
}
```

`router_test.go`:default 回退;Register 后命中;并发读写 race-clean。

- [ ] **Step 2: config NodeRegion**

env `WADIST_NODE_REGION` 默认 `"default"`。加默认值测试。

- [ ] **Step 3: main 装配**

`main.go`:
- 解码 cfg.MasterKey/BlindIndexKey;若启用安全(MasterKey 非空):`kr,_ := crypto.NewKeyRepo(mgr.SystemPool(), cfg.MasterKey)`;`cipher := crypto.NewCipher(kr)`;`aw := audit.NewAuditWriter(mgr.SystemPool())`;`router := crypto.NewRouter(mgr.Pool())`。
- billing:`billingRepo.UseTenantRLS(mgr)`(接入 RLS)+ 审计注入。
- 把 cipher/blindKey 传给需要 PII 双写的路径(目前 campaign 导入路径未在生产实现,仅 wiring 就绪即可;记 TODO)。
- MasterKey 为空(未配置安全)时:跳过 crypto 装配,保持现有行为(开发可无密钥启动),日志提示"security disabled"。

- [ ] **Step 4: smoke + 全门禁**

`main_smoke_test.go`:无 DSN skip;有 DSN(+可选 MASTER_KEY)run()→/metrics 200→stop 干净。
Run:
```bash
/usr/local/bin/go build ./... && /usr/local/bin/go vet ./...
./scripts/check_metric_labels.sh
TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./... -race
```
Expected: 全绿;门禁 OK。

- [ ] **Step 5: 提交**

```bash
git add internal/crypto/router.go internal/crypto/router_test.go internal/config/ cmd/wadist/
git commit -m "feat(crypto/cmd): data-residency Router stub + wire Cipher/KeyRepo/AuditWriter/RLS into main"
```

---

## Self-Review(写作后自查)

- **Spec 覆盖:** Cipher 信封+轮换+crypto-shred ✅(T1)、tenant_keys ✅(T1)、盲索引 ✅(T1)、RLS 双角色+WithTenant ✅(T2,机制完整)、billing 接入 RLS ✅(T2,用户选定范围)、PII 加密+盲索引双写暂留明文 ✅(T3,用户选定)、suppression_list 发送前拦截 ✅(T4)、append-only audit_log ✅(T4)、数据驻留 Router ✅(T5 桩)。
- **范围决策落实:** RLS 仅 billing 租户路径接入 + 其余分期债文档化(T2 Step6);PII 双写保留明文与旧 UNIQUE,新增盲索引唯一索引并存(T3)。
- **向后兼容:** RLS(billing txb nil→pool)、PII(Cipher 必需但仅新插入路径)、audit(nil→跳过)、suppression(加性查询)均加性;既有测试零回归;MasterKey 空→security disabled 不阻断启动。
- **密钥分层:** KEK(env)包裹 DEK(随机/租户),盲索引独立 HMAC 键;密文带版本前缀支持轮换;shred=删 DEK→ErrKeyNotFound。
- **RLS 正确性:** NOSUPERUSER app_tenant + FORCE RLS + `current_setting(...,true)` 缺设零行;WithTenant 用 set_config(local) 参数化;rls_test 用真实 app_tenant 角色连接证明跨租户隔离 + WITH CHECK 拒越权写。
- **append-only:** REVOKE UPDATE,DELETE on audit_log;测试以受限角色验证拒绝。
- **类型一致:** Cipher/KeyRepo/BlindIndex/WithTenant/TenantTxBeginner/AuditWriter/AuditEntry/Router 跨任务一致;config 新字段(MasterKey/BlindIndexKey/AppTenantDSN/AppSystemDSN/NodeRegion)。
- **占位:** 无 panic 占位;PII 生产导入路径未实现处仅记 TODO(wiring 就绪),不阻断。
- **迁移:** 0008 单文件分任务追加,全幂等(IF NOT EXISTS / DO $$ 角色守卫 / DROP POLICY IF EXISTS)。
