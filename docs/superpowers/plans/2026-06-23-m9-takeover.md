# M9 分布式接管 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现多节点分布式接管:增强 `DeviceLock.Healthy` 为 advisory-lock 所有权再校验(fencing 基石)、`cluster_nodes` 心跳、节点/账号所有权写入、`StartAccountWithLock` + `guardSession`(防双开)、`RunTakeoverScanner`(过期节点账号重抢,asynq Unique 去重)+ `TakeoverHandler`、优雅停机 `deregisterNode`。

**Architecture:** M1 遗留前置:`DeviceLock.Healthy` 当前仅 `conn.Ping`,会在连接被透明替换后误报存活 → 双开 → 封号。M9 改为在固定连接上查 `pg_locks`(`pid=pg_backend_pid()`)确认本 backend 仍持锁。编排逻辑放新包 `internal/node`(整合 cluster+store+asynq,允许 import 全部下层),whatsmeow 会话构建经注入 `SessionFactory` 抽象,使锁/心跳/扫描/接管/guard 全部可用真实 PG + fake 会话测试。接管以 advisory 锁为权威所有权令牌:`account_devices.owner_node`(已存在列)记录归属,`cluster_nodes` 记录心跳;扫描器找"归属于过期节点"的账号,经 asynq Unique 去重派发接管,handler 重抢锁并接管。

**Tech Stack:** Go 1.26.4、pgx/v5(pg_locks/advisory lock)、hibiken/asynq(Unique 去重)、go.mau.fi/whatsmeow(仅经 cluster 适配器)、golang.org/x/sync。

## Global Constraints

- **fencing 正确性(核心)**:`DeviceLock.Healthy` 必须确认本固定连接的 backend 仍持有 advisory 锁,而非仅连接存活。每个 `DeviceLock` 独占一条 lockPool 连接且仅取一把 advisory 锁——故"本 backend 上存在 granted advisory 锁"即等价于"仍持有本账号锁"(规避 int64 key 反解为 classid/objid 的符号位坑)。连接死亡或被替换 → 查询失败或 `pg_backend_pid()` 不同 → 返回 false。
- **防双开**:`guardSession` 周期调用 `Session.Healthy`(委托 `lock.Healthy`),一旦失活立即 `Registry.Remove` + `Session.Close`(断连+释锁),绝不让丢锁的会话继续收发。
- **接管去重**:跨节点共享 Redis,接管任务用 `asynq.Unique(ttl)`(键=type+payload+queue)实现集群级去重;`asynq.ErrDuplicateTask` 视为正常跳过。
- **停机顺序**:`deregisterNode`(清本节点 owner_node + 删 cluster_nodes 行)必须在**断会话之后**(锁已释放,避免对端看到指向仍持锁节点的 owner_node)、**关池之前**(需连接执行 UPDATE)。为此给 `cluster.SupervisorOpts` 新增 `AfterSessions func(ctx)` 钩子,在 `CloseAll` 与 `CloseStore` 之间调用。
- **分层**:新包 `internal/node` 允许 import cluster/store/dispatch/asynq/config(整合层);`cluster`/`store`/`dispatch`/`metrics` 不得 import `node`。whatsmeow 仍仅在 `cluster/conn_whatsmeow.go`。
- ctx 贯穿;`%w`;SQL 仅 `$N`;`-race` 干净;集成测试用真实 PG16(+Redis7);短模式 skip。高基数 metric 门禁保持绿。
- 不改已合并业务包既有导出签名(仅 `cluster.SupervisorOpts` 增字段 + `cluster.Supervisor.Shutdown` 增一调用点;`store` 增方法;`DeviceLock.Healthy` 签名不变)。
- module `github.com/acme/wadist`,Go 1.26.4,二进制 `/usr/local/bin/go`。迁移新增 `0007_cluster_nodes.sql`。

---

## File Structure

- `internal/store/lock.go` — 增强 `DeviceLock.Healthy`(pg_locks 所有权再校验)。
- `internal/store/lock_test.go` — 所有权有效/backend 终止后失活/释放后失活(真实 PG)。
- `migrations/0007_cluster_nodes.sql` — `cluster_nodes` + owner_node 索引 + 复用 touch 触发器。
- `internal/store/node.go` — `UpsertNodeHeartbeat`、`DeregisterNode`、`ListActiveAccounts`、`ClaimAccount`、`StaleOwnedAccounts`、`GetBoundProxy`。
- `internal/store/node_test.go` — 上述方法 + 迁移幂等(真实 PG)。
- `internal/config/config.go` — `HeartbeatInterval`(10s)、`NodeStaleness`(30s)、`TakeoverScanInterval`(15s)。
- `internal/cluster/supervisor.go` — `SupervisorOpts.AfterSessions func(ctx)` + Shutdown 调用点。
- `internal/cluster/supervisor_test.go` — 新顺序(intake→loops→sessions→AfterSessions→store→flush)。
- `internal/node/orchestrator.go` — `Orchestrator`、`SessionFactory`、`StartAccountWithLock`、`guardSession`。
- `internal/node/orchestrator_test.go` — claim+register、locked→skip、guard 失活移除(真实 PG + fake factory)。
- `internal/node/takeover.go` — `TypeTakeover`、`TakeoverEnqueuer`(Unique)、`RegisterTakeoverHandler`、`RunHeartbeat`、`RunTakeoverScanner`。
- `internal/node/takeover_test.go` — Unique 去重(真实 Redis)、scanner 派发过期账号(真实 PG+Redis)。
- `cmd/wadist/main.go` — 启动注册节点 + StartAccounts(ListActiveAccounts→StartAccountWithLock)+ 心跳/扫描循环 + 接管 handler + AfterSessions=deregister + 真实 SessionFactory(NewWAConn)。
- `cmd/wadist/main_smoke_test.go` — 启动/停机冒烟(含 deregister)。

---

### Task 1: 增强 DeviceLock.Healthy(advisory-lock 所有权再校验)

**Files:**
- Modify: `internal/store/lock.go`(Healthy 方法体)
- Modify: `internal/store/lock_test.go`(新增 3 个所有权测试)

**Interfaces:**
- Produces: `func (l *DeviceLock) Healthy(ctx context.Context) bool`(签名不变,语义增强)。`cluster.DeviceLockHandle` 自动受益,无需改 cluster。
- Consumes: 已有 `DeviceLock{conn *pgxpool.Conn; key int64}`、`advisoryKey`。

- [ ] **Step 1: 写失败测试(真实 PG,package store 内部测试可访问未导出字段)**

在 `internal/store/lock_test.go` 追加:

```go
func TestDeviceLock_Healthy_OwnershipValid(t *testing.T) {
	if testing.Short() {
		t.Skip("integration")
	}
	m := newTestManager(t) // 既有测试辅助：构造带 lockPool 的 *Manager（见现有 lock_test.go）
	ctx := context.Background()
	seedAccountDevice(t, ctx, m, "jid-h1") // 既有/新增最小 seed：插入一行 account_devices

	lock, err := m.AcquireDeviceLock(ctx, "jid-h1")
	if err != nil {
		t.Fatal(err)
	}
	defer lock.Release(ctx)
	if !lock.Healthy(ctx) {
		t.Fatal("freshly-acquired lock must be Healthy (owns advisory lock)")
	}
}

func TestDeviceLock_Healthy_FalseAfterBackendTerminated(t *testing.T) {
	if testing.Short() {
		t.Skip("integration")
	}
	m := newTestManager(t)
	ctx := context.Background()
	seedAccountDevice(t, ctx, m, "jid-h2")

	lock, err := m.AcquireDeviceLock(ctx, "jid-h2")
	if err != nil {
		t.Fatal(err)
	}
	defer lock.Release(ctx)
	// 取本锁固定连接的 backend pid（同包，可读未导出字段）
	pid := lock.conn.Conn().PgConn().PID()
	// 从另一条连接终止该 backend
	if _, err := m.bizPool.Exec(ctx, `SELECT pg_terminate_backend($1)`, pid); err != nil {
		t.Fatal(err)
	}
	if lock.Healthy(ctx) {
		t.Fatal("Healthy must be false after the lock's backend was terminated")
	}
}

func TestDeviceLock_Healthy_FalseAfterRelease(t *testing.T) {
	if testing.Short() {
		t.Skip("integration")
	}
	m := newTestManager(t)
	ctx := context.Background()
	seedAccountDevice(t, ctx, m, "jid-h3")
	lock, err := m.AcquireDeviceLock(ctx, "jid-h3")
	if err != nil {
		t.Fatal(err)
	}
	lock.Release(ctx)
	if lock.Healthy(ctx) {
		t.Fatal("Healthy must be false after Release (conn nil)")
	}
}
```

> **implementer:** 复用现有 `lock_test.go` 的 Manager 构造辅助(可能名为 `newTestManager`/`testManager`/直接 `newManager`;以现有为准)。`seedAccountDevice` 若不存在则加一个最小辅助:`INSERT INTO account_devices (tenant_id, account_jid, phone_number, ban_status) VALUES (1, $1, '+100', 'active')`(按 0001 NOT NULL 列补齐)。AcquireDeviceLock 仅需 jid 不要求该行存在,但 seed 让测试更贴近真实。

- [ ] **Step 2: 跑确认失败(旧 Ping 实现下 backend-terminated 测试应失败)**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/store/ -run TestDeviceLock_Healthy -v`
Expected: `FalseAfterBackendTerminated` 在旧实现下可能因 Ping 触发重连而表现不稳定 —— 这正是要修的;先确认新测试存在且 OwnershipValid 通过。

- [ ] **Step 3: 增强 Healthy**

替换 `internal/store/lock.go` 的 `Healthy` 方法体(保留方法签名与 docstring 顶部说明,删除原 M9 TODO 段,改为说明现已实现):

```go
// Healthy re-validates advisory-lock OWNERSHIP, not merely connection liveness.
// It queries pg_locks ON the pinned connection for a granted advisory lock held
// by THIS backend (pg_backend_pid()). Each DeviceLock pins its own dedicated
// connection and acquires exactly one advisory lock, so the presence of any
// granted advisory lock on this backend confirms we still own this account's
// lock. If the connection was transparently replaced, pg_backend_pid() is a
// different backend with no such lock -> false; if the connection is dead, the
// query errors -> false. This is the fencing primitive guardSession relies on to
// prevent double-open.
func (l *DeviceLock) Healthy(ctx context.Context) bool {
	if l == nil || l.conn == nil {
		return false
	}
	var ok bool
	err := l.conn.QueryRow(ctx, `
SELECT EXISTS (
  SELECT 1 FROM pg_locks
  WHERE locktype = 'advisory'
    AND pid = pg_backend_pid()
    AND granted
)`).Scan(&ok)
	return err == nil && ok
}
```

> **设计依据:** 依赖不变式"每个固定连接仅持一把 advisory 锁"。若未来在 lockPool 连接上引入多锁,需改为按 key 反解 `classid/objid` 匹配(注意 int64→两段 int32 的符号位)。本任务保持简单且正确。

- [ ] **Step 4: 跑测试(race)**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/store/ -run TestDeviceLock -race -v`
Expected: 三个新测试全 PASS;既有锁互斥测试仍 PASS。

- [ ] **Step 5: 提交**

```bash
git add internal/store/lock.go internal/store/lock_test.go
git commit -m "fix(store): DeviceLock.Healthy re-validates advisory-lock ownership via pg_locks (fencing)"
```

---

### Task 2: cluster_nodes 迁移 + 心跳/注销 + config

**Files:**
- Create: `migrations/0007_cluster_nodes.sql`
- Create: `internal/store/node.go`(UpsertNodeHeartbeat, DeregisterNode)
- Create: `internal/store/node_test.go`
- Modify: `internal/config/config.go`(三个间隔字段)

**Interfaces:**
- Produces: `func (m *Manager) UpsertNodeHeartbeat(ctx, nodeID string) error`;`func (m *Manager) DeregisterNode(ctx, nodeID string) error`;config `HeartbeatInterval/NodeStaleness/TakeoverScanInterval time.Duration`。

- [ ] **Step 1: 写迁移**

`migrations/0007_cluster_nodes.sql`:

```sql
-- 0007_cluster_nodes.sql — cluster membership heartbeat for distributed takeover.
CREATE TABLE IF NOT EXISTS cluster_nodes (
    node_id           TEXT PRIMARY KEY,
    last_heartbeat_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    started_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
    created_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at        TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_nodes_heartbeat ON cluster_nodes (last_heartbeat_at);
CREATE INDEX IF NOT EXISTS idx_acc_owner_node ON account_devices (owner_node) WHERE owner_node IS NOT NULL;

-- reuse touch_updated_at() defined in 0001
DROP TRIGGER IF EXISTS trg_nodes_touch ON cluster_nodes;
CREATE TRIGGER trg_nodes_touch BEFORE UPDATE ON cluster_nodes
    FOR EACH ROW EXECUTE FUNCTION touch_updated_at();
```

- [ ] **Step 2: 迁移幂等测试**

`internal/store/node_test.go`:

```go
func TestMigration0007_Idempotent(t *testing.T) {
	if testing.Short() {
		t.Skip("integration")
	}
	ctx := context.Background()
	pool := pgPool(t) // 既有真实 PG 辅助
	applyMigrations(t, ctx, pool)
	applyMigrations(t, ctx, pool) // 二次应用必须无错（IF NOT EXISTS / DROP IF EXISTS）
	var n int
	if err := pool.QueryRow(ctx, `SELECT count(*) FROM cluster_nodes`).Scan(&n); err != nil {
		t.Fatalf("cluster_nodes missing: %v", err)
	}
}
```

> implementer:复用 store 测试既有的 `pgPool`/`applyMigrations`(glob `../../migrations/*.sql`)。若 store 包用的是直接 DSN 辅助,匹配其风格。

- [ ] **Step 3: 心跳测试**

追加 `internal/store/node_test.go`:

```go
func TestUpsertNodeHeartbeat_InsertsAndUpdates(t *testing.T) {
	if testing.Short() {
		t.Skip("integration")
	}
	m := newTestManager(t)
	ctx := context.Background()
	if err := m.UpsertNodeHeartbeat(ctx, "node-a"); err != nil {
		t.Fatal(err)
	}
	var hb1 time.Time
	m.bizPool.QueryRow(ctx, `SELECT last_heartbeat_at FROM cluster_nodes WHERE node_id='node-a'`).Scan(&hb1)
	time.Sleep(10 * time.Millisecond)
	if err := m.UpsertNodeHeartbeat(ctx, "node-a"); err != nil { // upsert path
		t.Fatal(err)
	}
	var hb2 time.Time
	var cnt int
	m.bizPool.QueryRow(ctx, `SELECT count(*) FROM cluster_nodes WHERE node_id='node-a'`).Scan(&cnt)
	m.bizPool.QueryRow(ctx, `SELECT last_heartbeat_at FROM cluster_nodes WHERE node_id='node-a'`).Scan(&hb2)
	if cnt != 1 {
		t.Fatalf("expected 1 row, got %d", cnt)
	}
	if !hb2.After(hb1) {
		t.Fatal("second heartbeat must advance last_heartbeat_at")
	}
}

func TestDeregisterNode_ClearsOwnerAndRow(t *testing.T) {
	if testing.Short() {
		t.Skip("integration")
	}
	m := newTestManager(t)
	ctx := context.Background()
	m.UpsertNodeHeartbeat(ctx, "node-b")
	seedAccountDevice(t, ctx, m, "jid-own")
	m.bizPool.Exec(ctx, `UPDATE account_devices SET owner_node='node-b' WHERE account_jid='jid-own'`)

	if err := m.DeregisterNode(ctx, "node-b"); err != nil {
		t.Fatal(err)
	}
	var owner *string
	m.bizPool.QueryRow(ctx, `SELECT owner_node FROM account_devices WHERE account_jid='jid-own'`).Scan(&owner)
	if owner != nil {
		t.Fatalf("owner_node should be cleared, got %v", *owner)
	}
	var cnt int
	m.bizPool.QueryRow(ctx, `SELECT count(*) FROM cluster_nodes WHERE node_id='node-b'`).Scan(&cnt)
	if cnt != 0 {
		t.Fatal("node row should be deleted")
	}
}
```

- [ ] **Step 4: 写 node.go(心跳/注销)**

```go
// internal/store/node.go
package store

import (
	"context"
	"fmt"
)

// UpsertNodeHeartbeat records/refreshes this node's liveness in cluster_nodes.
func (m *Manager) UpsertNodeHeartbeat(ctx context.Context, nodeID string) error {
	_, err := m.bizPool.Exec(ctx, `
INSERT INTO cluster_nodes (node_id, last_heartbeat_at)
VALUES ($1, now())
ON CONFLICT (node_id) DO UPDATE SET last_heartbeat_at = now()`, nodeID)
	if err != nil {
		return fmt.Errorf("upsert node heartbeat: %w", err)
	}
	return nil
}

// DeregisterNode clears this node's account ownership and removes its membership
// row so peers do not wait out the staleness window on graceful shutdown. Called
// AFTER all sessions disconnected (advisory locks released) and BEFORE the pool
// closes.
func (m *Manager) DeregisterNode(ctx context.Context, nodeID string) error {
	if _, err := m.bizPool.Exec(ctx,
		`UPDATE account_devices SET owner_node = NULL WHERE owner_node = $1`, nodeID); err != nil {
		return fmt.Errorf("clear owner_node: %w", err)
	}
	if _, err := m.bizPool.Exec(ctx,
		`DELETE FROM cluster_nodes WHERE node_id = $1`, nodeID); err != nil {
		return fmt.Errorf("delete node: %w", err)
	}
	return nil
}
```

- [ ] **Step 5: config 三个间隔字段**

`config.go`:在 `Config` 增 `HeartbeatInterval`、`NodeStaleness`、`TakeoverScanInterval time.Duration`;`Load()` 用 `getenv`+`time.ParseDuration` 默认 `10s`/`30s`/`15s`(env `WADIST_HEARTBEAT_INTERVAL`/`WADIST_NODE_STALENESS`/`WADIST_TAKEOVER_SCAN_INTERVAL`,解析失败或 <=0 回退默认)。加一个 `TestLoad_TakeoverIntervalDefaults` 断言默认值。

- [ ] **Step 6: 跑测试(race)**

Run:
```bash
TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/store/ ./internal/config/ -race -v
```
Expected: 全 PASS。

- [ ] **Step 7: 提交**

```bash
git add migrations/0007_cluster_nodes.sql internal/store/node.go internal/store/node_test.go internal/config/
git commit -m "feat(store): cluster_nodes heartbeat + DeregisterNode + takeover interval config"
```

---

### Task 3: 账号所有权 + 启动/扫描查询

**Files:**
- Modify: `internal/store/node.go`(追加 4 方法)
- Modify: `internal/store/node_test.go`

**Interfaces:**
- Produces:
  - `func (m *Manager) ListActiveAccounts(ctx) ([]string, error)`
  - `func (m *Manager) ClaimAccount(ctx, accountJID, nodeID string) error`
  - `func (m *Manager) StaleOwnedAccounts(ctx, staleness time.Duration) ([]string, error)`
  - `func (m *Manager) GetBoundProxy(ctx, accountJID string) (*ProxyBinding, error)`

- [ ] **Step 1: 写失败测试**

追加 `internal/store/node_test.go`:

```go
func TestListActiveAccounts(t *testing.T) {
	if testing.Short() { t.Skip("integration") }
	m := newTestManager(t); ctx := context.Background()
	seedAccountDevice(t, ctx, m, "jid-a1")
	seedAccountDevice(t, ctx, m, "jid-a2")
	m.bizPool.Exec(ctx, `UPDATE account_devices SET ban_status='banned' WHERE account_jid='jid-a2'`)
	jids, err := m.ListActiveAccounts(ctx)
	if err != nil { t.Fatal(err) }
	// 只含 active
	found := map[string]bool{}
	for _, j := range jids { found[j] = true }
	if !found["jid-a1"] || found["jid-a2"] {
		t.Fatalf("active filter wrong: %v", jids)
	}
}

func TestClaimAccount(t *testing.T) {
	if testing.Short() { t.Skip("integration") }
	m := newTestManager(t); ctx := context.Background()
	seedAccountDevice(t, ctx, m, "jid-c1")
	if err := m.ClaimAccount(ctx, "jid-c1", "node-x"); err != nil { t.Fatal(err) }
	var owner string
	m.bizPool.QueryRow(ctx, `SELECT owner_node FROM account_devices WHERE account_jid='jid-c1'`).Scan(&owner)
	if owner != "node-x" { t.Fatalf("owner_node=%q", owner) }
}

func TestStaleOwnedAccounts(t *testing.T) {
	if testing.Short() { t.Skip("integration") }
	m := newTestManager(t); ctx := context.Background()
	// 过期节点 + 其名下 active 账号
	m.bizPool.Exec(ctx, `INSERT INTO cluster_nodes (node_id, last_heartbeat_at) VALUES ('dead-node', now() - interval '10 minutes')`)
	m.UpsertNodeHeartbeat(ctx, "live-node")
	seedAccountDevice(t, ctx, m, "jid-dead"); m.ClaimAccount(ctx, "jid-dead", "dead-node")
	seedAccountDevice(t, ctx, m, "jid-live"); m.ClaimAccount(ctx, "jid-live", "live-node")

	stale, err := m.StaleOwnedAccounts(ctx, 30*time.Second)
	if err != nil { t.Fatal(err) }
	found := map[string]bool{}
	for _, j := range stale { found[j] = true }
	if !found["jid-dead"] || found["jid-live"] {
		t.Fatalf("stale detection wrong: %v", stale)
	}
}
```

- [ ] **Step 2: 跑确认失败**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/store/ -run 'TestListActiveAccounts|TestClaimAccount|TestStaleOwnedAccounts' -v`
Expected: FAIL(undefined）

- [ ] **Step 3: 追加 node.go 方法**

```go
import (
	"context"
	"fmt"
	"time"
)

func (m *Manager) ListActiveAccounts(ctx context.Context) ([]string, error) {
	rows, err := m.bizPool.Query(ctx,
		`SELECT account_jid FROM account_devices WHERE ban_status='active' ORDER BY account_jid`)
	if err != nil {
		return nil, fmt.Errorf("list active accounts: %w", err)
	}
	defer rows.Close()
	var out []string
	for rows.Next() {
		var jid string
		if err := rows.Scan(&jid); err != nil {
			return nil, fmt.Errorf("scan account: %w", err)
		}
		out = append(out, jid)
	}
	return out, rows.Err()
}

func (m *Manager) ClaimAccount(ctx context.Context, accountJID, nodeID string) error {
	_, err := m.bizPool.Exec(ctx,
		`UPDATE account_devices SET owner_node=$2, last_connected_at=now() WHERE account_jid=$1`,
		accountJID, nodeID)
	if err != nil {
		return fmt.Errorf("claim account: %w", err)
	}
	return nil
}

// StaleOwnedAccounts returns active accounts whose owner node's heartbeat is
// older than `staleness` — candidates for takeover.
func (m *Manager) StaleOwnedAccounts(ctx context.Context, staleness time.Duration) ([]string, error) {
	rows, err := m.bizPool.Query(ctx, `
SELECT a.account_jid
  FROM account_devices a
  JOIN cluster_nodes n ON n.node_id = a.owner_node
 WHERE a.ban_status='active'
   AND n.last_heartbeat_at < now() - make_interval(secs => $1)
 ORDER BY a.account_jid`, staleness.Seconds())
	if err != nil {
		return nil, fmt.Errorf("stale owned accounts: %w", err)
	}
	defer rows.Close()
	var out []string
	for rows.Next() {
		var jid string
		if err := rows.Scan(&jid); err != nil {
			return nil, fmt.Errorf("scan stale account: %w", err)
		}
		out = append(out, jid)
	}
	return out, rows.Err()
}

// GetBoundProxy reads the cached proxy binding for an account (for reconnect).
// Returns ErrProxyNotBound if no proxy is cached.
func (m *Manager) GetBoundProxy(ctx context.Context, accountJID string) (*ProxyBinding, error) {
	// implementer: SELECT proxy_id, proxy_url_cache FROM account_devices WHERE account_jid=$1;
	// build a *ProxyBinding from those columns. Read the real ProxyBinding struct
	// (internal/store/proxy.go) and populate the field ApplyProxy needs (ProxyURL).
	// If proxy_url_cache IS NULL -> return a sentinel ErrProxyNotBound (define it).
	panic("implementer: fill from proxy.go ProxyBinding fields")
}
```

> **GetBoundProxy 说明:** 读 `internal/store/proxy.go` 的 `ProxyBinding` 真实字段与 `account_devices.proxy_url_cache`/`proxy_id`,返回最小可用 binding(`ApplyProxy` 只用 `ProxyURL`)。NULL 缓存 → 定义并返回 `ErrProxyNotBound`。补一个 `TestGetBoundProxy` 集成测试(seed 账号 + 绑定代理后读回)。**删除上面的 panic 占位**——它仅标注实现位置,最终代码不得保留 panic。

- [ ] **Step 4: 跑测试(race)**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/store/ -race -v`
Expected: 全 PASS。

- [ ] **Step 5: 提交**

```bash
git add internal/store/node.go internal/store/node_test.go
git commit -m "feat(store): account ownership claim + active/stale listing + bound-proxy readback"
```

---

### Task 4: node 编排 — StartAccountWithLock + guardSession

**Files:**
- Create: `internal/node/orchestrator.go`
- Create: `internal/node/orchestrator_test.go`

**Interfaces:**
- Consumes: `store.Manager`(AcquireDeviceLock/ClaimAccount/ErrDeviceLocked)、`cluster`(Registry/Supervisor/Session/DeviceLockHandle)。
- Produces:
  - `type SessionFactory func(ctx context.Context, jid string, lock cluster.DeviceLockHandle) (*cluster.Session, error)`
  - `type Orchestrator struct{...}`;`func NewOrchestrator(mgr *store.Manager, reg *cluster.Registry, sup *cluster.Supervisor, nodeID string, factory SessionFactory, guardInterval time.Duration) *Orchestrator`
  - `func (o *Orchestrator) StartAccountWithLock(ctx, jid string) (*cluster.Session, error)`(锁被他节点占用 → 返回 `(nil,nil)` 跳过)
  - `func (o *Orchestrator) guardSession(ctx, sess *cluster.Session)`(未导出;失活即移除+关闭)

- [ ] **Step 1: 写失败测试(真实 PG + fake factory)**

`internal/node/orchestrator_test.go`(自带 fake conn/lock,因 cluster 的 fake 不导出):

```go
package node

import (
	"context"
	"testing"
	"time"

	"github.com/acme/wadist/internal/cluster"
	"github.com/acme/wadist/internal/store"
)

type fakeConn struct{ connected bool }
func (f *fakeConn) Connect(_ context.Context) error { f.connected = true; return nil }
func (f *fakeConn) Disconnect()                      { f.connected = false }

// fake DeviceLockHandle that flips healthy on command (for guard test).
type fakeLock struct{ healthy bool; released bool }
func (l *fakeLock) Healthy(_ context.Context) bool { return l.healthy }
func (l *fakeLock) Release(_ context.Context)      { l.released = true }

func TestStartAccountWithLock_ClaimsAndRegisters(t *testing.T) {
	if testing.Short() { t.Skip("integration") }
	m := newTestManager(t)            // store test helper exported for node? -> see note
	ctx := context.Background()
	seedAccountDevice(t, ctx, m, "jid-s1")
	reg := cluster.NewRegistry()
	sup := cluster.NewSupervisor(reg, cluster.SupervisorOpts{Limit: 4, ShutdownTimeout: time.Second})
	factory := func(_ context.Context, jid string, lock cluster.DeviceLockHandle) (*cluster.Session, error) {
		return cluster.NewSession(jid, &fakeConn{connected: true}, lock), nil
	}
	o := NewOrchestrator(m, reg, sup, "node-1", factory, time.Second)
	sess, err := o.StartAccountWithLock(ctx, "jid-s1")
	if err != nil || sess == nil { t.Fatalf("start: sess=%v err=%v", sess, err) }
	if _, ok := reg.Get("jid-s1"); !ok { t.Fatal("not registered") }
	var owner string
	m.bizPool.QueryRow(ctx, `SELECT owner_node FROM account_devices WHERE account_jid='jid-s1'`).Scan(&owner)
	if owner != "node-1" { t.Fatalf("owner_node=%q", owner) }
	sup.Shutdown()
}

func TestStartAccountWithLock_SkipsWhenLockedByPeer(t *testing.T) {
	if testing.Short() { t.Skip("integration") }
	m := newTestManager(t); ctx := context.Background()
	seedAccountDevice(t, ctx, m, "jid-s2")
	// 另一连接先占锁
	peerLock, err := m.AcquireDeviceLock(ctx, "jid-s2")
	if err != nil { t.Fatal(err) }
	defer peerLock.Release(ctx)
	reg := cluster.NewRegistry()
	sup := cluster.NewSupervisor(reg, cluster.SupervisorOpts{Limit: 4, ShutdownTimeout: time.Second})
	factory := func(_ context.Context, jid string, lock cluster.DeviceLockHandle) (*cluster.Session, error) {
		t.Fatal("factory must not be called when lock is held by a peer"); return nil, nil
	}
	o := NewOrchestrator(m, reg, sup, "node-2", factory, time.Second)
	sess, err := o.StartAccountWithLock(ctx, "jid-s2")
	if err != nil || sess != nil { t.Fatalf("expected (nil,nil) skip, got sess=%v err=%v", sess, err) }
	if reg.Len() != 0 { t.Fatal("must not register when locked by peer") }
}

func TestGuardSession_RemovesOnUnhealthy(t *testing.T) {
	reg := cluster.NewRegistry()
	sup := cluster.NewSupervisor(reg, cluster.SupervisorOpts{Limit: 4, ShutdownTimeout: time.Second})
	fl := &fakeLock{healthy: true}
	sess := cluster.NewSession("jid-g", &fakeConn{connected: true}, fl)
	reg.Add(sess)
	o := NewOrchestrator(nil, reg, sup, "node-g", nil, 5*time.Millisecond)
	go o.guardSession(context.Background(), sess)
	time.Sleep(20 * time.Millisecond)
	fl.healthy = false // simulate lost ownership
	deadline := time.After(time.Second)
	for {
		if _, ok := reg.Get("jid-g"); !ok { break }
		select {
		case <-deadline:
			t.Fatal("guard did not remove unhealthy session")
		default:
			time.Sleep(5 * time.Millisecond)
		}
	}
	if !fl.released { t.Fatal("lock must be released on guard close") }
}
```

> **测试辅助说明(给 implementer):** node 包测试需要一个真实 `*store.Manager` + seed。`store` 的 testcontainer 辅助在 `store` 包的 `_test.go`(不可跨包用)。两种做法,择一并说明:(a) 在 `internal/store` 增加一个**导出**的测试辅助构造(如 `store.NewTestManager(t)` 放在非 `_test.go` 但 build tag `//go:build testhelpers` 的文件)——较重;(b) **推荐**:在 `internal/node/orchestrator_test.go` 内联一个最小 testcontainers PG 启动 + `store.Init`(或 store 暴露的非单例构造)+ glob 迁移,自给自足(复制 ~30 行 testcontainer 样板,与其它包一致)。`seedAccountDevice` 同样内联。`newTestManager` 即该辅助。务必让 node 测试自洽运行真实 PG。

- [ ] **Step 2: 跑确认失败**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/node/ -run 'TestStartAccountWithLock|TestGuardSession' -v`
Expected: FAIL(包不存在)

- [ ] **Step 3: 写 orchestrator.go**

```go
// Package node is the integration layer that composes store (locks/ownership),
// cluster (sessions/registry/supervisor), and asynq (takeover) into the
// distributed account-takeover machinery. It is the only package allowed to
// import all of those; cluster/store/dispatch must NOT import node.
package node

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/acme/wadist/internal/cluster"
	"github.com/acme/wadist/internal/store"
)

// SessionFactory builds and connects a Session for an account using a held lock.
// The real factory (in cmd) does GetDeviceStore + GetBoundProxy + NewWAConn +
// Connect + NewSession; tests inject a fake returning a fakeConn-backed Session.
type SessionFactory func(ctx context.Context, jid string, lock cluster.DeviceLockHandle) (*cluster.Session, error)

type Orchestrator struct {
	mgr           *store.Manager
	reg           *cluster.Registry
	sup           *cluster.Supervisor
	nodeID        string
	factory       SessionFactory
	guardInterval time.Duration
}

func NewOrchestrator(mgr *store.Manager, reg *cluster.Registry, sup *cluster.Supervisor, nodeID string, factory SessionFactory, guardInterval time.Duration) *Orchestrator {
	if guardInterval <= 0 {
		guardInterval = 10 * time.Second
	}
	return &Orchestrator{mgr: mgr, reg: reg, sup: sup, nodeID: nodeID, factory: factory, guardInterval: guardInterval}
}

// StartAccountWithLock acquires the account's advisory lock, builds+connects its
// session, claims ownership, registers it, and spawns a fencing guard. If
// another node already holds the lock it returns (nil, nil) — caller skips.
func (o *Orchestrator) StartAccountWithLock(ctx context.Context, jid string) (*cluster.Session, error) {
	lock, err := o.mgr.AcquireDeviceLock(ctx, jid)
	if errors.Is(err, store.ErrDeviceLocked) {
		return nil, nil // owned by a live peer
	}
	if err != nil {
		return nil, fmt.Errorf("acquire lock %s: %w", jid, err)
	}
	sess, err := o.factory(ctx, jid, lock)
	if err != nil {
		lock.Release(ctx)
		return nil, fmt.Errorf("start session %s: %w", jid, err)
	}
	if err := o.mgr.ClaimAccount(ctx, jid, o.nodeID); err != nil {
		sess.Close(ctx) // disconnects + releases lock
		return nil, fmt.Errorf("claim %s: %w", jid, err)
	}
	o.reg.Add(sess)
	o.sup.Go(func(gctx context.Context) error {
		o.guardSession(gctx, sess)
		return nil
	})
	return sess, nil
}

// guardSession fences against double-open: if the session loses advisory-lock
// ownership (transparent reconnect / backend death), remove it from the registry
// and close it so no further sends/receives occur on a lock we no longer hold.
func (o *Orchestrator) guardSession(ctx context.Context, sess *cluster.Session) {
	t := time.NewTicker(o.guardInterval)
	defer t.Stop()
	for {
		select {
		case <-ctx.Done():
			return
		case <-t.C:
			if !sess.Healthy(ctx) {
				o.reg.Remove(sess.JID())
				sess.Close(ctx)
				return
			}
		}
	}
}
```

> **note:** `store.AcquireDeviceLock` 返回 `*store.DeviceLock`,它满足 `cluster.DeviceLockHandle`(Healthy/Release),故可直接作为 factory 的 `lock` 参数与 Session 的锁。确认 `store.ErrDeviceLocked` 已导出(M1 已有)。

- [ ] **Step 4: 跑测试(race)**

Run: `TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./internal/node/ -race -v`
Expected: 全 PASS(start claim+register;peer-locked skip;guard 移除)。

- [ ] **Step 5: 提交**

```bash
git add internal/node/orchestrator.go internal/node/orchestrator_test.go
git commit -m "feat(node): StartAccountWithLock + guardSession fencing (lock-acquire, claim, register, guard)"
```

---

### Task 5: 接管扫描/handler(asynq Unique)+ deregister 钩子 + main 总装

**Files:**
- Create: `internal/node/takeover.go`
- Create: `internal/node/takeover_test.go`
- Modify: `internal/cluster/supervisor.go`(+ `AfterSessions` 钩子)
- Modify: `internal/cluster/supervisor_test.go`(顺序)
- Modify: `cmd/wadist/main.go`、`cmd/wadist/main_smoke_test.go`

**Interfaces:**
- Produces:
  - `const TypeTakeover = "cluster:takeover"`
  - `func NewTakeoverEnqueuer(client *asynq.Client, uniqueTTL time.Duration) *TakeoverEnqueuer`;`func (e *TakeoverEnqueuer) Enqueue(ctx, jid string) error`(Unique;`asynq.ErrDuplicateTask` 吞掉返回 nil)
  - `func RegisterTakeoverHandler(mux *asynq.ServeMux, o *Orchestrator)`(handler 解 jid → `o.StartAccountWithLock`)
  - `func (o *Orchestrator) RunHeartbeat(ctx, interval time.Duration) error`(受管循环体)
  - `func (o *Orchestrator) RunTakeoverScanner(ctx, scanInterval, staleness time.Duration, enq *TakeoverEnqueuer) error`
  - cluster:`SupervisorOpts.AfterSessions func(ctx context.Context)`

- [ ] **Step 1: cluster Supervisor 增 AfterSessions 钩子 + 测试**

改 `internal/cluster/supervisor.go`:`SupervisorOpts` 增 `AfterSessions func(ctx context.Context)`;在 `Shutdown` 的 `reg.CloseAll(ctx)` 之后、`CloseStore()` 之前调用:

```go
// 3. sessions
ctx, cancel := context.WithTimeout(context.Background(), s.opts.ShutdownTimeout)
s.reg.CloseAll(ctx)
// 3a. after sessions detached (locks released), before store closes
if s.opts.AfterSessions != nil {
	s.opts.AfterSessions(ctx)
}
cancel()
// 4. store
...
```

改 `supervisor_test.go` 的 `TestSupervisor_ShutdownOrder`:在 opts 增 `AfterSessions: func(_ context.Context){ rec("deregister") }`,断言顺序 `intake → deregister → store → flush`(会话断开仍夹在 intake 与 deregister 之间,由 conn.connected==false 验证)。

- [ ] **Step 2: takeover 失败测试(真实 Redis + PG)**

`internal/node/takeover_test.go`:

```go
package node

import (
	"context"
	"errors"
	"testing"
	"time"

	"github.com/hibiken/asynq"
)

func TestTakeoverEnqueuer_Unique(t *testing.T) {
	if testing.Short() { t.Skip("integration") }
	redisAddr := startRedis(t) // 内联 testcontainer redis:7，返回 addr
	client := asynq.NewClient(asynq.RedisClientOpt{Addr: redisAddr})
	defer client.Close()
	enq := NewTakeoverEnqueuer(client, 30*time.Second)
	ctx := context.Background()
	if err := enq.Enqueue(ctx, "jid-t1"); err != nil {
		t.Fatalf("first enqueue: %v", err)
	}
	// 第二次同 jid 在 TTL 内 → 去重，Enqueue 吞掉 ErrDuplicateTask 返回 nil
	if err := enq.Enqueue(ctx, "jid-t1"); err != nil {
		t.Fatalf("duplicate enqueue should be swallowed, got %v", err)
	}
	_ = errors.Is
}

func TestRunTakeoverScanner_EnqueuesStale(t *testing.T) {
	if testing.Short() { t.Skip("integration") }
	m := newTestManager(t); ctx := context.Background()
	redisAddr := startRedis(t)
	client := asynq.NewClient(asynq.RedisClientOpt{Addr: redisAddr}); defer client.Close()
	// 过期节点 + 名下 active 账号
	m.bizPool.Exec(ctx, `INSERT INTO cluster_nodes (node_id, last_heartbeat_at) VALUES ('dead', now() - interval '10 minutes')`)
	seedAccountDevice(t, ctx, m, "jid-stale"); m.ClaimAccount(ctx, "jid-stale", "dead")

	o := NewOrchestrator(m, nil, nil, "node-scan", nil, time.Second)
	enq := NewTakeoverEnqueuer(client, 30*time.Second)
	// 单次扫描：用一个会立即取消的 ctx 跑一拍，或直接调内部 scanOnce
	n, err := o.scanOnce(ctx, 30*time.Second, enq) // 见实现：scanOnce 返回入队数
	if err != nil { t.Fatal(err) }
	if n < 1 { t.Fatalf("expected >=1 stale enqueued, got %d", n) }
}
```

> implementer:`startRedis` 内联 testcontainer(参照 dispatch 测试)。把扫描主体抽成 `scanOnce(ctx, staleness, enq)(int,error)`(查 StaleOwnedAccounts → 逐个 enq.Enqueue,统计成功入队数),`RunTakeoverScanner` 为其 ticker 包装,便于单测。

- [ ] **Step 3: 写 takeover.go**

```go
// internal/node/takeover.go
package node

import (
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"time"

	"github.com/hibiken/asynq"
)

const TypeTakeover = "cluster:takeover"

type takeoverPayload struct {
	JID string `json:"jid"`
}

type TakeoverEnqueuer struct {
	client    *asynq.Client
	uniqueTTL time.Duration
}

func NewTakeoverEnqueuer(client *asynq.Client, uniqueTTL time.Duration) *TakeoverEnqueuer {
	if uniqueTTL < time.Second {
		uniqueTTL = time.Second
	}
	return &TakeoverEnqueuer{client: client, uniqueTTL: uniqueTTL}
}

// Enqueue submits a takeover task, deduplicated cluster-wide via asynq.Unique
// (key = type+payload+queue over shared Redis). A duplicate within the TTL is
// swallowed (returns nil) — another scan/node already queued it.
func (e *TakeoverEnqueuer) Enqueue(ctx context.Context, jid string) error {
	b, err := json.Marshal(takeoverPayload{JID: jid})
	if err != nil {
		return fmt.Errorf("marshal takeover: %w", err)
	}
	_, err = e.client.EnqueueContext(ctx, asynq.NewTask(TypeTakeover, b),
		asynq.Queue("takeover"), asynq.Unique(e.uniqueTTL))
	if errors.Is(err, asynq.ErrDuplicateTask) {
		return nil
	}
	if err != nil {
		return fmt.Errorf("enqueue takeover: %w", err)
	}
	return nil
}

// RegisterTakeoverHandler wires the takeover task to StartAccountWithLock.
func RegisterTakeoverHandler(mux *asynq.ServeMux, o *Orchestrator) {
	mux.HandleFunc(TypeTakeover, func(ctx context.Context, t *asynq.Task) error {
		var p takeoverPayload
		if err := json.Unmarshal(t.Payload(), &p); err != nil {
			return fmt.Errorf("unmarshal takeover: %w", err)
		}
		_, err := o.StartAccountWithLock(ctx, p.JID)
		return err // nil if skipped (peer holds lock) — task done
	})
}

// RunHeartbeat is a managed loop body: refresh this node's heartbeat each tick.
func (o *Orchestrator) RunHeartbeat(ctx context.Context, interval time.Duration) error {
	t := time.NewTicker(interval)
	defer t.Stop()
	for {
		select {
		case <-ctx.Done():
			return ctx.Err()
		case <-t.C:
			if err := o.mgr.UpsertNodeHeartbeat(ctx, o.nodeID); err != nil {
				// transient — log-and-continue (next tick retries)
			}
		}
	}
}

func (o *Orchestrator) scanOnce(ctx context.Context, staleness time.Duration, enq *TakeoverEnqueuer) (int, error) {
	jids, err := o.mgr.StaleOwnedAccounts(ctx, staleness)
	if err != nil {
		return 0, fmt.Errorf("scan stale: %w", err)
	}
	n := 0
	for _, jid := range jids {
		if err := enq.Enqueue(ctx, jid); err != nil {
			return n, fmt.Errorf("enqueue takeover %s: %w", jid, err)
		}
		n++
	}
	return n, nil
}

// RunTakeoverScanner is a managed loop: each tick, enqueue takeover for accounts
// owned by stale nodes (deduped via Unique).
func (o *Orchestrator) RunTakeoverScanner(ctx context.Context, scanInterval, staleness time.Duration, enq *TakeoverEnqueuer) error {
	t := time.NewTicker(scanInterval)
	defer t.Stop()
	for {
		select {
		case <-ctx.Done():
			return ctx.Err()
		case <-t.C:
			if _, err := o.scanOnce(ctx, staleness, enq); err != nil {
				// transient — log-and-continue
			}
		}
	}
}
```

> implementer:确认 asynq v0.26.0 `Client.EnqueueContext(ctx, task, ...Option)` 存在(否则用 `Enqueue` + `asynq.ProcessIn`/无 ctx);`asynq.Unique`、`asynq.Queue`、`asynq.ErrDuplicateTask` 均 v0.26.0 API(侦察已确认)。takeover 队列需在 asynq.Server 的 Queues 配置中启用(main 里 `asynq.Config{Queues: map[string]int{"default":..,"takeover":..}}` 或保持默认队列——若用默认队列则去掉 `asynq.Queue("takeover")`,统一走 default;**择一并保证 server 消费该队列**)。

- [ ] **Step 4: main 总装(per 下述要点)**

改 `cmd/wadist/main.go`:
1. 启动时注册本节点:`mgr.UpsertNodeHeartbeat(ctx, cfg.NodeID)`。
2. 真实 SessionFactory:
```go
factory := func(fctx context.Context, jid string, lock cluster.DeviceLockHandle) (*cluster.Session, error) {
	device, err := mgr.GetDeviceStore(fctx, jid)
	if err != nil { return nil, err }
	proxy, err := mgr.GetBoundProxy(fctx, jid)
	if err != nil { return nil, err } // 或容忍无代理：proxy=nil
	conn := cluster.NewWAConn(device, logger, proxy)
	if err := conn.Connect(fctx); err != nil { return nil, err }
	return cluster.NewSession(jid, conn, lock), nil
}
```
3. `orch := node.NewOrchestrator(mgr, reg, sup, cfg.NodeID, factory, cfg.HeartbeatInterval)`(guardInterval 可用独立默认;若复用 HeartbeatInterval 亦可)。
4. 接管 enqueuer + handler:`takeEnq := node.NewTakeoverEnqueuer(asynqClient, cfg.NodeStaleness)`;`node.RegisterTakeoverHandler(mux, orch)`(在 `asynqSrv.Run` 前注册到同一 mux)。
5. 启动期接管所有应在本节点跑的账号:
```go
jids, _ := mgr.ListActiveAccounts(ctx)
go func() { _ = sup.StartAccounts(ctx, jids, orch.StartAccountWithLock) }() // 后台并发起号（SetLimit 限流）
```
> 注意 `sup.StartAccounts` 的 start 形参签名是 `func(ctx, jid)(*Session,error)`,`orch.StartAccountWithLock` 正好匹配(锁占用返回 (nil,nil),StartAccounts 把 nil session 视为成功不入册——确认 StartAccounts 对 nil session 的处理:M8 的 StartAccounts 在 `start` 成功后 `reg.Add(sess)`;若 sess 为 nil 会 panic。**因此 main 包一层**:`start := func(c,j){ s,e:=orch.StartAccountWithLock(c,j); return s,e }` 不行,仍 nil。**修复:** 让 StartAccounts 容忍 nil(M8 改动)或在 orchestrator 包一个 `startForSupervisor` 跳过 nil。最简:不经 sup.StartAccounts,自己用 errgroup 调 StartAccountWithLock 并忽略 nil。implementer 选择并说明;推荐给 cluster.Supervisor.StartAccounts 加 nil-session 跳过(s!=nil 才 Add),并补一行测试。)
6. 受管循环:`sup.Go(func(c){ return orch.RunHeartbeat(c, cfg.HeartbeatInterval) })`;`sup.Go(func(c){ return orch.RunTakeoverScanner(c, cfg.TakeoverScanInterval, cfg.NodeStaleness, takeEnq) })`。
7. `SupervisorOpts.AfterSessions = func(c context.Context){ _ = mgr.DeregisterNode(c, cfg.NodeID) }`(在构造 Supervisor 时设入)。
8. RoutingSender 仍为 worker 的 Sender(M8);现在有真实会话入册后,发送可路由到活跃会话。
9. asynq server 若用 "takeover" 队列,配置 `Queues` 含之;否则统一 default。

- [ ] **Step 5: 更新冒烟测试**

`main_smoke_test.go`:仍 skip without DSN;有 DSN 时 run()→GET /metrics 200 含 `wadist_`→stop() 干净(stop 经 sup.Shutdown 触发 AfterSessions=DeregisterNode,不 panic)。可额外断言 stop 后 cluster_nodes 无本节点行(若便于)。

- [ ] **Step 6: 全门禁**

Run:
```bash
/usr/local/bin/go build ./... && /usr/local/bin/go vet ./...
./scripts/check_metric_labels.sh
TESTCONTAINERS_RYUK_DISABLED=true /usr/local/bin/go test ./... -race
```
Expected: 全绿;高基数门禁 OK。

- [ ] **Step 7: 提交**

```bash
git add internal/node/takeover.go internal/node/takeover_test.go internal/cluster/supervisor.go internal/cluster/supervisor_test.go cmd/wadist/
git commit -m "feat(node): takeover scanner+handler (asynq Unique) + heartbeat loop + deregister hook + main wiring"
```

---

## Self-Review(写作后自查)

- **Spec 覆盖:** 增强 Healthy 所有权再校验 ✅(T1,M1 前置收口)、cluster_nodes + RunHeartbeat ✅(T2/T5)、owner_node 写入/清除 ✅(T2/T3)、StartAccountWithLock + guardSession 防双开 ✅(T4)、RunTakeoverScanner(asynq Unique)+ TakeoverHandler ✅(T5)、deregisterNode(AfterSessions 钩子,断会话后/关池前)✅(T5)、StartAccounts SetLimit 起号 ✅(T5 复用 M8)。
- **可测性:** 锁/心跳/所有权/扫描全真实 PG;Unique 真实 Redis;会话构建经 SessionFactory 注入,guard/start 用 fakeConn/fakeLock;无需真实 whatsmeow。
- **fencing 正确性:** Healthy 在固定连接查 `pg_locks pid=pg_backend_pid()`,依赖"每连接仅一把 advisory 锁"不变式(已注明若违背则需 key 反解)。测试用 `pg_terminate_backend` 验证失活。
- **无环:** node→{cluster,store,dispatch,asynq,config};cluster/store/dispatch/metrics 不 import node;whatsmeow 仍仅 cluster 适配器。
- **停机:** AfterSessions 在 CloseAll 后、CloseStore 前;deregister 清 owner_node + 删节点行;心跳/扫描 goroutine 由 sup.Go 在 step2 取消。
- **类型一致:** Orchestrator/SessionFactory/StartAccountWithLock/guardSession/TakeoverEnqueuer/RunHeartbeat/RunTakeoverScanner/scanOnce、SupervisorOpts.AfterSessions 跨任务一致。
- **遗留风险标注:** StartAccounts 对 nil session(锁被占)需跳过——T5 要求给 cluster.Supervisor.StartAccounts 加 `if sess != nil` 守卫并补测(否则 nil.Add panic)。这是对 M8 的最小且必要修正。
- **占位:** T3 的 GetBoundProxy 含 `panic("implementer...")` 仅标注位置,**最终代码必须删除 panic 并按真实 ProxyBinding 字段实现**;已在步骤显式要求。
